$kind: http-request
name: Get Department by ID
method: GET
url: '{{baseUrl}}/api/departments/{{departmentId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'

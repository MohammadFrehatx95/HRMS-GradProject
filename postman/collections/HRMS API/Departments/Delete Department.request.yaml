$kind: http-request
name: Delete Department (Admin)
method: DELETE
url: '{{baseUrl}}/api/departments/{{departmentId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'

$kind: http-request
name: Delete Position (Admin)
method: DELETE
url: '{{baseUrl}}/api/positions/{{positionId}}'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'

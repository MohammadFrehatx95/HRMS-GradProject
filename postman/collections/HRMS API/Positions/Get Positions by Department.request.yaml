$kind: http-request
name: Get Positions by Department
method: GET
url: '{{baseUrl}}/api/positions/department/{{departmentId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
